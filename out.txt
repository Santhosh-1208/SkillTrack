<USER_REQUEST>
[9:12 am, 14/08/2026] Kce Santhosh M: 1)I want you to add a field called isHidden for simulations in mongodb 
and set isHidden = true for All simulations in db Except safety simulations and simulation that are using docker terminals.
Important Note you should set isHidden=false for safety simulations and simulations that are using docker terminals.
2)See the site is using browser's alert box when i click every delete actions.
I want it to use its own alert box inside the browser
[9:25 am, 14/08/2026] Kce Santhosh M: Before we add any 3D visualization, I need to confirm all four safety simulations are actually working correctly end-to-end: PPE_COMPLIANCE_001, HAZARD_IDENT_002, LOTO_PROC_004, FIRE_EVAC_003.

Do NOT modify any files yet. This is a read-only investigation task.

For each of the four simulations, check and report back on:

1. Does the simulation load correctly in the runner page without errors (console errors, missing data, broken SOP step rendering)?
2. Do all SOP steps render their instruction text correctly and in the right order?
3. Do decision points actually trigger at the correct step (after_step field), display all options, and let the learner select one?
4. When an option is selected, does the correct is_correct / consequence / mistake_id logic fire as expected — i.e., does picking a wrong option actually apply the mistake_penalty and log a mistake?
5. Does the scoring system correctly calculate a final score using base_score, pass_threshold, and the penalties from possible_mistakes?
6. Are there any hardcoded assumptions in the simulation runner component that only work for TERMINAL-type simulations (like the Git one) but might break for PROCEDURAL or VISUAL type simulations (since these four safety sims are PROCEDURAL/VISUAL, not TERMINAL)?
7. Any console errors, missing imports, or broken references when navigating to each of these four simulations specifically?

Give me a clear pass/fail report for each of the four simulations across these checks, with specific file names and line numbers for anything broken. Do not attempt fixes yet — just report findings first.

---

STOP after the report. Wait for me to review the findings and give explicit approval before making any fixes.

Once I approve, fix ONLY the specific errors found — do not refactor, rename, or restructure anything beyond what's needed to resolve each reported issue. Show me each fix individually (one simulation at a time) so I can confirm it didn't break anything else before moving to the next.

Once all four simulations are confirmed error-free and I've explicitly said to proceed, THEN begin the 3D visualization integration task:

I have an existing SkillTrack project with safety simulations (PPE_COMPLIANCE_001, HAZARD_IDENT_002, LOTO_PROC_004, FIRE_EVAC_003) now working end-to-end — SOP steps, decision points, scoring, and mistake tracking are all functional and MUST NOT be modified, broken, or have their logic altered in any way.

TASK: Add a React Three Fiber 3D visualization panel alongside each of these four safety simulations, purely as a visual layer that reacts to existing state — it must not introduce new state, new scoring logic, or change any existing data flow.

REQUIREMENTS:

1. Do not touch any SOP validation, scoring, decision-point logic, mistake tracking, or backend/API calls. Treat all of that as read-only.

2. Create a new, isolated component (e.g., SafetySimulationViewer.jsx or similar, following this project's existing naming conventions) that:
   - Accepts the current simulation_id, current sop_step_id, and the selected_option_id (if a decision point was just answered) as props — read from existing state, don't create new global state.
   - Renders a <Canvas> (React Three Fiber) scene that updates based on those props only.
   - Uses a single shared scene-state mapping (simulation_id + step_id + option_id → visual state) so the four simulations can share reusable primitives (hazard icon billboard, path arrows, checkmark sprites, etc).

3. Implement the exact per-step and per-decision visual changes as specified in the visualization spec I provide separately for each of the four simulations.

4. Mount this new component alongside the existing SOP/terminal panel in each simulation's runner page — as an additional panel (e.g., split view or tab), not replacing any existing UI element.

5. Before writing any code for this part: read the existing simulation runner page component, the SOP step state management (wherever step/decision state currently lives — context, hook, or props), and confirm how selected_option_id is currently exposed after a learner picks a decision option. Show me a short summary of what you find before making changes.

6. After implementing, do not modify any test files, scoring thresholds, or mistake_penalty values. If you need a new library (three, @react-three/fiber, @react-three/drei), add it to package.json only — do not touch other dependencies.

7. Make changes incrementally: implement and show me one simulation (start with PPE_COMPLIANCE_001) fully working before moving to the next three, so I can verify nothing broke before you proceed.

Confirm your understanding of the existing state flow first, then proceed with PPE_COMPLIANCE_001 only.

═══════════════════════════════════════════════════════════════════
SAFETY SIMULATION 3D VISUALIZATION SPEC — React Three Fiber
═══════════════════════════════════════════════════════════════════

──────────────────────────────────────────
1. FIRE_EVAC_003 — Fire Emergency Response
──────────────────────────────────────────
BASE SCENE: Office/warehouse interior — desks, corridor, exit doors, fire
alarm pull station, extinguisher mount, assembly point marker outside.

FIRE_S1 (Identify fire source):
  - Small orange/red emissive particle cluster (smoke shader, low opacity)
    appears at one source point (e.g., a waste bin near a desk).
  - Camera/learner marker highlights the source with a pulsing ring.

FIRE_S2 (Activate pull station):
  - Pull station model animates lever from UP to DOWN position.
  - Ceiling strobe lights begin flashing (red emissive pulse).
  - Ambient audio cue: alarm siren (non-visual, but trigger flag for sound).

FIRE_S3 (Call emergency services):
  - Small phone/radio icon overlay appears near learner avatar (UI sprite,
    not scene geometry) — no major 3D change, static confirmation checkmark.

FIRE_S4 (Assess fire size) → DECISION POINT FIRE_D1:
  - Fire particle system scales up to "desk-sized" — larger flame mesh,
    denser smoke column reaching ceiling, orange point-light with flicker.
  - Option A (Fight it – incorrect): Learner avatar animates walking toward
    fire with extinguisher; smoke column intensifies, flame mesh scales up
    further, screen edge vignettes red, avatar model shown stumbling back —
    visualizes the "trapped/failed" outcome without depicting injury.
  - Option B (Evacuate – correct): Fire and smoke stay contained to source
    zone; avatar animates turning away and moving toward corridor; a green
    directional path arrow (floating chevrons) appears leading to exit.

FIRE_S5 (Alert colleagues):
  - 2–3 NPC avatar meshes near desks change idle pose to "alerted" pose
    (standing up) and begin following the learner's path arrow.

FIRE_S6 (Navigate to exit) → DECISION POINT FIRE_D2:
  - Corridor mesh gets a smoke volume (particle fog, gray, semi-transparent)
    filling the primary exit path at chest-height.
  - Two path arrows appear: short one through smoke (primary), longer one
    around it (secondary).
  - Option A (Run through smoke – incorrect): Camera/avatar height stays
    upright moving through dense fog volume; screen desaturates and a
    red "visibility low" overlay pulses — visualizes hazard, not harm.
  - Option B (Crawl to secondary – correct): Avatar animation switches to
    a low crouch/crawl pose; camera lowers to near-floor height, fog
    volume visibly thins below waist height; path arrow (green) redirects
    to secondary exit.

FIRE_S7 (Close fire doors):
  - Each fire door mesh along the path animates open→closed as the avatar
    passes through it (hinge rotation animation).

FIRE_S8 (Assembly point + headcount):
  - Scene cuts to exterior assembly zone; NPC avatars cluster near a
    marked ground zone; a headcount counter UI increments as each NPC
    "checks in" (small checkmark billboard sprite over each avatar).


──────────────────────────────────────────
2. HAZARD_IDENT_002 — Workplace Hazard Identification
──────────────────────────────────────────
BASE SCENE: Factory floor — walkways, machinery row, exit door, fire
extinguisher wall mount, chemical storage cabinet, overhead lighting rig.

HAZ_S1 (Get checklist):
  - Clipboard/checklist UI panel appears in learner's hand (attached mesh
    or HUD icon), 0/8 items checked.

HAZ_S2 (Scan floor hazards):
  - Highlight (yellow outline shader) toggles on 2–3 planted hazard props:
    a liquid spill decal, a loose cable coiled on the floor, a misplaced
    tool. Learner can click each to "flag" it — flagged props get a red
    warning icon billboard.

HAZ_S3 (Inspect exits) → DECISION POINT HAZ_D1:
  - A pallet-of-boxes mesh is stacked directly in front of the exit door,
    partially blocking it (door swing arc intersects the boxes).
  - Option A (Note for later – incorrect): Boxes remain in place; door
    highlight turns red/locked icon persists; a "hazard still active"
    marker stays on screen as learner moves to next step.
  - Option B (Move it now – correct): Pallet mesh animates sliding away
    from the door path; door highlight turns green; exit sign glow
    brightens to indicate "clear."

HAZ_S4 (Inspect extinguishers):
  - Extinguisher pressure gauge needle mesh rotates to show green zone;
    inspection tag mesh flips to reveal current date texture; small
    green checkmark billboard appears above extinguisher.

HAZ_S5 (Inspect machinery):
  - One machine mesh has a missing guard panel — exposed rotating gear
    submesh (dulled/matte, non-textured "danger" material) is visible.
  - Clicking it flags it (red warning icon), and a semi-transparent
    "correct guard" ghost-mesh overlay shows what should be there.

HAZ_S6 (Chemical storage) → DECISION POINT HAZ_D2:
  - Storage cabinet shows two identical unlabeled containers (plain
    gray material, no label texture) sitting side-by-side on same shelf.
  - Option A (Assume same, leave – incorrect): Containers remain
    together; a subtle orange hazard-symbol particle effect (warning,
    not explosion) hovers between them, persisting as unresolved.
  - Option B (Segregate + tag – correct): Containers animate sliding
    apart to opposite ends of the cabinet; a new label mesh/texture
    "UNKNOWN — DO NOT USE" (bright yellow/black) applies to both.

HAZ_S7 (Lighting check):
  - Overhead light meshes cycle through states — one flickers (emissive
    intensity oscillates) to indicate a faulty fixture; clicking it logs
    the hazard and the flicker stops (fixture "flagged," not fixed).

HAZ_S8 (Log hazards):
  - Checklist HUD panel fills to 8/8; each logged hazard gets a small
    colored severity tag (yellow/orange/red dot) in a summary list panel.


──────────────────────────────────────────
3. LOTO_PROC_004 — Lockout/Tagout (LOTO) Procedure
──────────────────────────────────────────
BASE SCENE: Industrial machine (e.g., conveyor/press) with visible main
breaker panel, pneumatic line, control panel, padlock/tag station nearby.

LOTO_S1 (Review procedure sheet):
  - Procedure sheet mesh/UI panel opens showing this machine's specific
    steps (static reference panel, highlights current machine's ID).

LOTO_S2 (Notify personnel):
  - NPC avatars near the machine animate turning toward learner and
    stepping back from the work zone (acknowledgment nod animation).

LOTO_S3 (Normal shutdown):
  - Machine's moving parts (conveyor belt texture UV scroll, rotating
    shaft) animate to a stop; status light mesh changes emissive color
    from green (running) to amber (stopping) to off.

LOTO_S4 (Isolate main breaker) → DECISION POINT LOTO_D1:
  - Breaker panel door opens (hinge animation); breaker switch mesh
    flips from ON to OFF (rotation animation); panel indicator light
    turns off.
  - A secondary pneumatic line prop (visible pipe with pressure gauge)
    is highlighted, gauge needle still showing pressure.
  - Option A (Ignore it – incorrect): Pneumatic gauge needle stays in
    pressurized zone (red arc); a persistent orange hazard icon hovers
    over the line as learner proceeds — visual flag of unresolved risk.
  - Option B (Close valve + lockout – correct): Valve handle mesh
    rotates 90° to closed position; gauge needle animates dropping to
    zero; a small padlock mesh clips onto the valve lockout device.

LOTO_S5 (Apply padlock):
  - Padlock mesh animates from learner's hand attaching to breaker
    hasp; shackle closes (rotation + snap animation); padlock now
    fixed to breaker.

LOTO_S6 (Attach tag):
  - Tag mesh (with placeholder name/date texture, dynamically updated
    text via canvas texture) animates swinging onto the padlock's
    wire loop.

LOTO_S7 (Dissipate stored energy):
  - Capacitor/pneumatic line props show a "discharging" particle
    effect (small diminishing spark/air-release puff) that fades out;
    any gravity-held part (e.g., raised arm/ram) animates lowering
    onto a mechanical block/support prop.

LOTO_S8 (Verify zero energy) → DECISION POINT LOTO_D2:
  - Learner avatar presses the machine start button — machine mesh
    stays static (no movement), confirming isolation, green "no
    response" indicator flashes briefly.
  - Voltmeter prop mesh appears with probe touching isolation point;
    digital readout texture shows "6V" instead of "0V" (red text).
  - Option A (Proceed anyway – incorrect): Voltmeter stays clipped to
    the point, readout persists at "6V" in red, a warning icon remains
    active as learner moves toward the machine — visualizes the
    unresolved hazard state, not an injury.
  - Option B (Stop, investigate – correct): Voltmeter is traced along
    the circuit path (camera follows probe to a secondary source
    prop); once found, readout animates dropping to "0V" in green.


──────────────────────────────────────────
4. PPE_COMPLIANCE_001 — PPE Compliance & Inspection
──────────────────────────────────────────
BASE SCENE: PPE locker room — wall rack with helmets, goggles, gloves,
footwear bins; full-length mirror; checkpoint scanner arch at exit.

PPE_S1 (Identify hazard zone/required PPE):
  - Signage mesh above the entry displays required PPE icons (helmet,
    goggles, gloves, boots) — icons light up one by one as "required."

PPE_S2 (Select helmet):
  - Learner avatar hand reaches to rack; helmet mesh detaches and
    attaches to avatar's hand (simple grab animation).

PPE_S3 (Inspect helmet) → DECISION POINT PPE_D1:
  - Helmet mesh rotates in learner's hand (inspection view); a hairline
    crack decal near the rim becomes visible on close rotation.
  - Option A (Wear anyway – incorrect): Helmet animates onto avatar's
    head; crack decal remains visible in a close-up mirror reflection
    shader, persistent as an unresolved flag icon above the head.
  - Option B (Discard + replace – correct): Cracked helmet mesh
    animates being placed in a red "damaged PPE" bin prop; a new
    intact helmet detaches from rack and attaches to learner's hand.
  - Option C (Tape over – incorrect): A tape strip decal applies over
    the crack; helmet still flagged (icon persists) — visualizes that
    the fix is cosmetic only, crack outline still faintly visible
    beneath the tape texture.

PPE_S4 (Goggles):
  - Goggles mesh selected, held up to light (small lens-flare/shine
    shader toggle) to check for scratches, then attaches to helmet.

PPE_S5 (Gloves) → DECISION POINT PPE_D2:
  - Glove rack shows only one bin available: "Cut-Resistant" (labeled
    mesh/texture). A solvent container prop sits nearby indicating the
    actual task hazard (chemical, not cut).
  - Option A (Use cut-resistant – incorrect): Gloves animate onto
    avatar hands; when hand approaches solvent container, a subtle
    orange "incompatible PPE" icon pulses above the gloves — visual
    mismatch flag, not simulated harm.
  - Option B (Request chemical-rated – correct): Avatar animates
    walking to a supervisor NPC; NPC hands over a new pair of gloves
    with a "Chemical Rated" label texture; these attach to avatar
    hands instead, icon turns green.

PPE_S6 (Footwear):
  - Boots mesh selected from bin; sole underside view rotates to
    inspect tread (procedural wear-pattern shader check), then attach
    to avatar feet.

PPE_S7 (Don all PPE, verify fit):
  - Full avatar model updates in mirror reflection showing all PPE
    equipped; straps/seals animate tightening (small clip/buckle
    animations on helmet strap, glove cuffs, boot laces).

PPE_S8 (Checkpoint scanner):
  - Avatar walks through scanner arch; arch mesh emissive color
    sweeps top-to-bottom (scan animation); on pass, arch glows green
    and a checkmark holographic icon appears; gate/barrier prop
    animates opening to allow entry.

═══════════════════════════════════════════════════════════════════
IMPLEMENTATION NOTES (apply across all four):
- Use a shared <SceneStateProvider> context that maps sopStepId +
  selectedOptionId → a scene-state object (visible props, material
  overrides, animation triggers) so each simulation's R3F canvas just
  subscribes to current SOP/decision state rather than re-rendering
  the whole scene tree.
- Incorrect-option visuals should always show an unresolved/hazard
  indicator (icon, color, particle) rather than a harm/injury
  outcome — keeps consequences legible without depicting injury.
- Correct-option visuals should always resolve the hazard indicator
  fully (icon disappears or turns green) so learners get unambiguous
  positive reinforcement.
- Reuse one hazard-icon billboard component (red/orange/green states)
  across all four sims for consistency and lower asset overhead.
═══════════════════════════════════════════════════════════════════
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-14T09:26:25+05:30.
</ADDITIONAL_METADATA>
